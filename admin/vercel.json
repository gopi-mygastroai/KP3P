{
  "buildCommand": "npm run seed:rulebook-text && npx prisma generate && next build",
  "installCommand": "npm ci",
  "framework": "nextjs",
  "functions": {
    "src/app/api/generate-caresheet/route.ts": {
      "maxDuration": 300
    }
  }
}
