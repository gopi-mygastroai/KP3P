{
    "buildCommand": "cd admin && npx prisma migrate deploy && npx prisma generate && next build",
    "installCommand": "cd admin && npm ci",
    "outputDirectory": "admin/.next",
    "framework": "nextjs"
  }